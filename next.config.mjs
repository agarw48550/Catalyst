/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router is default in Next.js 15+
  // No need to set output directory - Next.js manages this automatically
  // For static export, uncomment: output: 'export'
};

export default nextConfig;
